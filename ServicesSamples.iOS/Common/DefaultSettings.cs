using CoreGraphics;
using Intersoft.Crosslight.iOS;

namespace ServicesSamples.iOS
{
    public class DefaultSettings
    {
        #region Fields

        public static readonly ImageSettings ImageSettings = new ImageSettings()
        {
            ImageSize = new CGSize(36, 36)
        };

        #endregion
    }
}