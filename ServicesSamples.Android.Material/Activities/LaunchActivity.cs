using Android.App;
using Intersoft.Crosslight.Android;

namespace ServicesSamples.Android
{
    [Activity(Label = "ServicesSamples.Android", MainLauncher = true, NoHistory = true, Icon = "@mipmap/icon", Theme = "@style/Theme.Splash")]
    public class LaunchActivity : StartActivity
    {
    }
}